#include <cstdio>
#include "foo.h"

int main(){
	// vector<int> ss = generatePrime(20);
	// for(size_t i=0;i<ss.size();i++){
	// 	printf("%d\n",ss[i]);
	// }
 	
 // 	int x = gcd(2,20);
 // 	printf("gcd(2,20) = %d\n",x);

 // 	int y = power(2, 20);
 // 	printf("2^20 = %d\n",y);

 // 	bignum z = 22223432;
 // 	printf("z = %s\n",z.str().c_str());

 // 	bignum z2 = "111111111";
 // 	printf("z2 = %s\n",z2.str().c_str());
 // 	cout << "z2 = " << z2 << endl ;

 // 	bignum z3 = z + z2;
 // 	cout << z3 << endl;
 // 	cout << z3 - z2 << endl;


 // 	z = z2;
 // 	cout << "z = " << z <<"| z2 <<" << z2 << endl;
 // 	z = z + bignum("20000");
 // 	cout << "z = " << z <<"| z2 <<" << z2 << endl;

 // 	cout << bignum("22222") / 2 << endl;

 // 	int x0,y0,g;
 // 	printf("%d\n",findSolutionDiophantine(5,3,1,x0,y0,g));
 // 	printf("x0 = %d, y0 = %d, g = %d\n",x0,y0,g);
	return 0;
}
